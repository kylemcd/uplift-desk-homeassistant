# Contributing

Protocol reports must include the advertised UUIDs, resolved service and
characteristic UUIDs, controller model/firmware when available, and whether
the command was observed passively or exercised on hardware. Never mark a
command verified for a profile based only on another desk variant.

Run `npm run check` before opening a pull request.
